{% extends "base.html.twig" %}

{% block title%} Créer un campus {% endblock %}

{% block stylesheets%} <link rel="stylesheet" href="{{ asset("css/form.css") }}">{% endblock %}

{% block body %}
    <h1>Créer un campus</h1>
    <div>
        {{ form_start(campusForm) }}
            {{ form_row(campusForm.nom_campus) }}
        {{ form_end(campusForm) }}
    </div>
{% endblock %}