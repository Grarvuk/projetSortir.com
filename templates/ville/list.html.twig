{% extends "base.html.twig" %}

{% block title%} Liste des villes {% endblock %}

{% block stylesheets%} <link rel="stylesheet" href="{{ asset("css/profile.css") }}">{% endblock %}
{% block javascript%} 
    <script src="{{ asset("js/detail.js") }}"></script>
{% endblock %}
{% block body %}
    <h1>Liste des villes</h1>

    <table id="myTable" class="display" style="width:100%">
            <thead>
                <tr>
                    <th>Nom de la ville</th>
                </tr>
            </thead>
            <tbody>
                {% for ville in villes %}
                    <tr>
                        <td><a href="{{ path('ville_detail', {'id' : ville.id}) }}">{{ ville.getNomVille() }}</a></td>
                    </tr>
                {% endfor%}
            </tbody>
            <tfoot>
            </tfoot>
        </table>

    <div>
        
    <a href="{{ path('ville_insert') }}" class="btn btn-btn-default btn-ajout"><svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24"><path d="M12 0c-6.627 0-12 5.373-12 12s5.373 12 12 12 12-5.373 12-12-5.373-12-12-12zm6 13h-5v5h-2v-5h-5v-2h5v-5h2v5h5v2z"/></svg> 
    Ajouter une ville</a>
    </div>
{% endblock %}